# Libraries the notebooks install into the Fabric Spark session.
#
# Preferred: attach the shared Fabric Environment described in
# docs/00-prerequisites.md, which pre-installs these and removes the
# two-to-four minute %pip install wait at the top of every notebook.
#
# Fallback: the first cell of each notebook runs
#   %pip install -q -r <this list>
# which is slower but needs no admin help.

pystac-client>=0.8.3
planetary-computer>=1.0.0
odc-stac>=0.3.9
rioxarray>=0.15.0
rasterio>=1.3.9
xarray>=2024.3.0
geopandas>=0.14.3
shapely>=2.0.3
pyproj>=3.6.1
rasterstats>=0.19.0
scikit-learn>=1.4.0
contextily>=1.6.0
openai>=1.30.0
